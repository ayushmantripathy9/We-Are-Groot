# Media Files
location /api_media/ {
    autoindex       on;
    alias           /groot_media/;
    expires         1d;
    access_log      off;
}

# Static Files
location /api_static/ {
    autoindex       on;
    alias           /groot_static/;
    expires         1d;
    access_log      off;
}